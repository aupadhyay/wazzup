{
  "name": "thoughts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "pnpm --filter @thoughts/db exec drizzle-kit generate",
    "db:migrate": "pnpm --filter @thoughts/db exec drizzle-kit migrate",
    "db:push": "pnpm --filter @thoughts/db exec drizzle-kit push",
    "db:studio": "pnpm --filter @thoughts/db exec drizzle-kit studio",
    "db:check": "pnpm --filter @thoughts/db exec drizzle-kit check",
    "build:rpc": "pnpm --filter @thoughts/rpc build",
    "build:all": "pnpm build:rpc && pnpm --filter @thoughts/desktop build",
    "pipeline:run": "pnpm --filter @thoughts/pipeline run",
    "pipeline:reset": "pnpm --filter @thoughts/pipeline reset"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "biome": "^0.3.3",
    "prettier": "^3.6.2"
  }
}